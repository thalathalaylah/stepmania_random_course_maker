#COURSE:<%= course_name %>
#SCRIPTER:<%= auther %>
#METER:<%= meter %>

<% songs.each do |song| %>
#SONG:<%= song.relative_dir %>:<%= song.difficulties.find{|di| di.level == target_level}.difficulty_str %>:nodifficult,<%= song.speed(target_bpms_array) %>x;
<% end %>
